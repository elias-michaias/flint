type person.

alice :: person.
bob :: person.
carol :: person.

parent :: person -> person -> type.
ancestor :: person -> person -> type.

!p1 :: parent(alice, bob).
!p2 :: parent(bob, carol).

ancestor_base :: parent($x, $y) => ancestor($x, $y).

?- ancestor($x, carol).
